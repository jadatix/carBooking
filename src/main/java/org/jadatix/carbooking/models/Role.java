package org.jadatix.carbooking.models;

import javax.persistence.Entity;

public enum Role {
    // Manager to provide access customer to car
    MANAGER,
    // User that is customer to want rent a car
    USER
}
